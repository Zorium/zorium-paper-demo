move vars.json to npm
add normalize.css back in
all components should be completely independent (remove root.styl and vars dep)
refactor to $el and $$el
mixin support instead of class extension (ripple)
